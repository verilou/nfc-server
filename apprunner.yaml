version: 1.0
runtime: nodejs16
build:
  commands:    
    build:
      - npm install --production
      - npm i -g pm2                             
run:                              
  command: npx pm2 app.js