version: 1.0
runtime: nodejs16
build:
  commands:    
    build:
      - npm install --production
      - npm i pm2 -g                             
run:                   
  network:
    port: 3000
    env: APP_PORT           
  command: npx pm2 start server.js
 