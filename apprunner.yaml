version: 1.0
runtime: nodejs16
build:
    commands:
        pre-build:
            - npm i pm2 -g
        build:
            - npm install --production
run:
    network:
        port: 3000
        env: APP_PORT
    command: 'node server.js'
    env:
        - name: SECRET_DB_HOST
          value: prod-db-name

        - name: SECRET_RDS
          value: rds!db-f6488152-e9bc-4ff7-a4b5-aa75d124f76e
